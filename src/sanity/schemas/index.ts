import { dessert } from './dessert';
import { category } from './category';
import { siteSettings } from './siteSettings';
import { order } from './order';

export const schemaTypes = [dessert, category, siteSettings, order];
